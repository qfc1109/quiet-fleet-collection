# 项目进度

## 计划索引

- 第一版设计规格：[2026-07-01-quiet-fleet-collection-design.md](superpowers/specs/2026-07-01-quiet-fleet-collection-design.md)
- 第一版实现计划：[2026-07-01-quiet-fleet-collection-v1.md](superpowers/plans/2026-07-01-quiet-fleet-collection-v1.md)

## 需求：开发轻帆集 / Quiet Fleet Collection 第一版 <2026-07-01 14-18-28>：

- 【✔】 阶段 1：明确第一版产品范围；结果：确定公开首页、项目广场、项目展示页、登录页、个人空间、项目式文件组织、核心文件预览范围；影响范围：需求分析、设计方案；验证：未运行，原因：仅完成需求确认和文档整理
- 【✔】 阶段 2：建立项目文档；结果：创建需求、设计、开发、进度和设计规格文档；影响范围：`docs/`；验证：人工重读文档，用户已在后续上下文确认进入实现计划
- 【✔】 阶段 3：创建实现计划；结果：新增 `docs/superpowers/plans/2026-07-01-quiet-fleet-collection-v1.md`；影响范围：实现路线、阶段拆分、验收命令；验证：扫描 `TBD/TODO/implement later/fill in details/placeholder/待确认`，未命中
- 【✔】 阶段 4：初始化 Spring Boot + Vue 3 项目；结果：新增 `server/` Spring Boot 2.7.18 + Maven 骨架，新增 `web/` Vue 3 + TypeScript + Vite 骨架，新增 `.gitignore`；影响范围：后端入口、后端配置、后端 smoke test、前端路由、Pinia store、Axios client、基础页面；验证：`mvn -f server/pom.xml test -B -ntp` 通过，`npm --prefix web run build` 通过，`npm --prefix web run dev -- --host 127.0.0.1 --port 5173` 已启动并通过浏览器检查基础路由
- 【✔】 阶段 5：实现后端数据库、登录、项目和文件接口；结果：完成 MySQL 5.7 schema、BCrypt 管理员初始化、统一返回结构、登录接口、公开项目接口、管理项目接口、文件上传、公开/管理文件列表、预览类型映射、Markdown/Excel 预览、PDF/图片 inline 内容接口、下载接口、删除文件接口；影响范围：`server/src/main/java/com/qfc`、`server/src/main/resources/db`、后端测试；验证：先写文件能力测试，首次运行因 `FileService` 等类缺失失败，补实现后 `mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`
- 【✔】 阶段 6：实现前端公开页、登录页和个人空间；结果：完成前端 API 模块，首页/用户页读取公开项目，项目页读取项目详情和文件列表，文件预览页支持 Markdown、PDF、图片、Excel 和下载入口，登录页调用后端登录，个人空间支持创建项目、选择项目、上传文件、查看文件列表和打开公开页；影响范围：`web/src/api`、`web/src/views`、`web/src/stores`、`web/src/style.css`；验证：`npm --prefix web run build` 通过，浏览器检查 `/`、`/login`、`/space`、`/p/example`、`/p/example/files/1` 均可渲染且控制台无错误
- 【✔】 阶段 6.1：支持仓库式文件夹上传和 Markdown 相对图片；结果：`project_file` 新增 `relative_path`，后端按 `storage/projects/<project-id>/<relative-path>` 保存文件，上传接口支持可选 `relativePath`，同一路径重复上传会更新原文件记录，公开资源接口支持 `GET /api/public/files/{fileId}/assets?path=<relative-path>`，Markdown 预览会把相对图片改写为同项目资源地址；个人空间支持上传普通文件和整个文件夹，文件列表显示项目内路径，Markdown 使用 Marked + DOMPurify 渲染；影响范围：`server/src/main/java/com/qfc/file`、`server/src/main/resources/db/schema.sql`、`server/src/test/java/com/qfc/file/FileServiceTest.java`、`web/src/api`、`web/src/views`、`web/src/style.css`、`web/package.json`、`docs/development.md`；验证：先写文件夹上传、非法路径、Markdown 图片改写、资源读取和重复路径上传测试，首次运行因字段/方法缺失或仍走 insert 失败，补实现后 `mvn -f server/pom.xml -Dtest=FileServiceTest test -B -ntp` 通过，`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 通过
- 【✔】 阶段 7：联调上传、预览、下载和公开访问流程；结果：使用本机 Docker MySQL 5.7 初始化 `quiet_fleet_collection` 数据库并执行 `schema.sql`、`data.sql`，后端因本机 `8080` 已被其他 nginx 占用改为 `8081`，前端 Vite 代理改为 `8081`；完成 `admin / 123456` 登录、创建公开示例项目、上传包含图片的 Markdown 文件夹、公开文件列表、Markdown 预览和图片资源读取验证；影响范围：`server/src/main/resources/application.yml`、`web/vite.config.ts`、本机 MySQL 数据库、运行进程；验证：接口返回 `loginUser=admin`、`projectId=1`、`fileCount=2`、`markdownImageRewritten=true`、图片资源接口 `HTTP/1.1 200`
- 【✔】 阶段 8：运行验证并整理结果；结果：最小版本当前可通过 `http://127.0.0.1:5173/p/example` 访问公开示例项目，通过 `http://127.0.0.1:5173/p/example/files/1` 访问 Markdown 预览；浏览器检查公开项目页显示 2 个文件，Markdown 预览页渲染成功并显示 1 张图片，控制台无前端 error；验证：`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 15, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 通过；剩余风险：数据库密码需要通过本机配置文件提供，真实配置不提交；当前 UI 仍是最小可用版本，未做树形文件浏览和后台管理
- 【✔】 修复 2026-07-01 17:28 个人空间登录态和上传目标问题；结果：顶部导航登录后显示 `admin` 和退出按钮，未登录时隐藏“个人空间”入口，直接访问 `/space` 会跳转 `/login?redirect=/space`；创建项目表单有草稿时上传文件/文件夹按钮禁用，并提示先创建当前项目，草稿会保存在浏览器本地，刷新不丢，避免文件误传到下拉框选中的旧项目；影响范围：`web/src/App.vue`、`web/src/router/index.ts`、`web/src/views/LoginView.vue`、`web/src/views/SpaceView.vue`、`web/src/style.css`；验证：浏览器检查登录态导航、退出后的路由守卫、项目草稿刷新保留和上传按钮禁用均符合预期；`npm --prefix web run build` 通过；`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 15, Failures: 0, Errors: 0, Skipped: 0`
- 【✔】 修正 2026-07-01 18:00 首页管理入口和上传交互；结果：移除首页“管理员登录”按钮，公开首页只保留项目广场入口；撤销项目草稿 localStorage 保存和草稿状态禁用上传的交互，上传文件/文件夹按钮仅在未选择项目或正在上传时禁用，上传完成只局部刷新文件列表，不刷新页面，不清空项目表单；文件上传区明确显示“上传目标”；影响范围：`web/src/views/HomeView.vue`、`web/src/views/SpaceView.vue`；验证：浏览器检查首页无“管理员登录”，quick actions 仅“进入项目广场”；个人空间填写项目表单后上传按钮仍可用且表单值保留；`npm --prefix web run build` 通过
- 【✔】 修正 2026-07-01 18:35 文件列表和删除文件交互；结果：新增目录树构建工具和 `FileTreeList` 组件，个人空间文件列表按文件夹目录展示并支持删除文件，公开项目页复用只读目录树；删除前弹出确认框，删除后只更新当前文件列表，不刷新整页；影响范围：`web/src/utils/fileTree.ts`、`web/tests/fileTree.test.mjs`、`web/src/components/FileTreeList.vue`、`web/src/api/admin.ts`、`web/src/views/SpaceView.vue`、`web/src/views/ProjectView.vue`、`web/src/style.css`；验证：目录树测试先因工具文件缺失失败，补实现后通过；浏览器检查 `/space` 目录树显示 2 个目录、9 个文件、9 个删除按钮，上传临时文件后通过页面删除并确认后列表移除且后端公开文件列表无测试文件；浏览器检查 `/p/111` 目录树显示 2 个目录、9 个文件、0 个删除按钮；控制台无 error；`npm --prefix web run build` 通过；`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 15, Failures: 0, Errors: 0, Skipped: 0`
- 【✔】 修正 2026-07-01 18:45 个人空间项目管理体验；结果：目录树目录行支持收放；个人空间改为以项目列表为主，已有项目全部显示，点击项目后进入详情区编辑项目名、slug、简介和排序，并在同一区域管理文件；创建项目改为按钮入口和弹窗表单；无项目时显示居中的创建入口和基础引导；影响范围：`web/src/utils/fileTree.ts`、`web/tests/fileTree.test.mjs`、`web/src/components/FileTreeList.vue`、`web/src/api/admin.ts`、`web/src/views/SpaceView.vue`、`web/src/style.css`；验证：目录树收放测试先因 `visibleFileTreeRows` 缺失失败，补实现后通过；浏览器检查 `/space` 初始显示 2 个项目、无常驻创建表单，点击“小画文档”后出现项目编辑区和 9 个文件，折叠顶层目录后文件数从 9 变 0、展开后恢复 9，创建项目按钮打开弹窗且取消可关闭，保存当前项目原值后提示“项目信息已保存”；`npm --prefix web run build` 通过
- 【✔】 新增 2026-07-01 19:35 后台管理最小版和登录默认值修复；结果：登录页账号和密码默认均为空；新增早期主站后台管理页，包含网站用户、后台管理员、角色权限三个模块；后端新增 `account_type`、`status`、`admin_role`、`permission`、`user_role`、`role_permission`，初始化 `SUPER_ADMIN`、普通管理员角色和 7 个权限点；`admin` 保持为 `ADMIN / ENABLED` 并绑定 `SUPER_ADMIN`；`/api/admin/**` 从仅校验登录升级为按后台权限拦截；影响范围：`server/src/main/java/com/qfc/admin`、`server/src/main/java/com/qfc/auth`、`server/src/main/java/com/qfc/config/AdminAuthInterceptor.java`、`server/src/main/resources/db`、`server/src/test/java/com/qfc/admin`、`web/src/api`、`web/src/router/index.ts`、`web/src/stores/session.ts`、`web/src/views/AdminView.vue`、`web/src/views/LoginView.vue`、`web/src/App.vue`、`web/src/style.css`、`docs/`；验证：先写 RBAC/用户管理测试，首次运行因新类和字段缺失失败，补实现后 `mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 20, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 通过；执行 `migration-2026-07-01-admin-rbac.sql` 后接口验证 `admin` 返回 `SUPER_ADMIN` 和全部权限；浏览器检查登录默认输入为空，后台三个模块可加载且角色权限显示正常
- 【✔】 修正 2026-07-01 19:50 公开项目入口命名；结果：将公开项目入口改为“项目广场”，页面标题、导航和首页入口统一表达为公开项目发现页；新增 `/explore` 路由，旧 `/u` 自动重定向到 `/explore`；影响范围：`web/src/views/ExploreView.vue`、`web/src/router/index.ts`、`web/src/App.vue`、`web/src/views/HomeView.vue`、`docs/`；验证：`npm --prefix web run build` 通过；旧名称和旧视图文件名扫描无业务文案残留；浏览器检查访问 `/u` 自动跳转 `/explore`，导航和页面标题显示“项目广场”，页面无旧名称残留
- 【✔】 修正 2026-07-01 20:10 文件预览页文档阅读体验；结果：文件预览页新增返回项目页按钮，Markdown 预览会抽取标题生成标题索引并给正文标题生成可跳转锚点，重复标题自动去重；影响范围：`web/src/views/FilePreviewView.vue`、`web/src/utils/markdownToc.ts`、`web/tests/markdownToc.test.ts`、`web/src/style.css`、`docs/`；验证：标题索引测试先因 `markdownToc.ts` 缺失失败，补实现后 `node --experimental-strip-types tests/markdownToc.test.ts` 通过；`npm --prefix web run build` 通过；浏览器检查 `/p/111/files/14` 桌面宽度下左侧显示 6 个标题索引且全部有对应正文目标，点击“报错”跳转到对应标题，点击“返回”从 hash 位置直接回到 `/p/111`
- 【✔】 修正 2026-07-01 21:20 独立后台、注册入口和固定角色；结果：主站移除后台管理路由和导航入口，登录页提供登录/注册切换与独立后台登录入口；新增 `admin-web/` 独立后台前端；后端新增 `POST /api/auth/register`；后台角色固定为 `SUPER_ADMIN` 和 `NORMAL_ADMIN`，创建角色接口返回 `ROLE_CREATION_DISABLED`，超级管理员拥有全部 7 项权限；SQL 脚本增加 `SET NAMES utf8mb4` 并新增乱码与角色修正迁移脚本；影响范围：`server/src/main/java/com/qfc/auth`、`server/src/main/java/com/qfc/admin`、`server/src/main/resources/db`、`server/src/test/java/com/qfc`、`web/src`、`admin-web/`、`docs/`；验证：接口检查 `admin / 123456` 返回 `轻帆管理员`、`SUPER_ADMIN` 和 7 项权限；浏览器检查主站登录页标题为“轻帆集”、输入为空、有注册和后台入口、无后台管理导航；后台登录后中文无乱码，角色页只显示 `SUPER_ADMIN` 和 `NORMAL_ADMIN` 且没有“新增角色”
- 【✔】 修正 2026-07-02 09:13 账号中心整合和头像上传；结果：主站移除单独“个人空间”导航，顶部账号入口进入 `/space` 账号中心；账号中心增加昵称、简介和头像上传，项目公开入口文案改为“访客视角”；后端新增当前账号资料和头像接口，头像保存到 `storage/avatars/<user-id>/avatar.<ext>` 并通过公开头像接口读取；影响范围：`server/src/main/java/com/qfc/auth`、`server/src/test/java/com/qfc/auth/AuthServiceTest.java`、`web/src/App.vue`、`web/src/views/SpaceView.vue`、`web/src/api`、`web/src/stores/session.ts`、`web/src/style.css`、`docs/`；验证：`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 24, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 通过；接口检查登录、`/api/auth/me`、`PUT /api/auth/profile` 和公开项目列表均返回 `SUCCESS`；浏览器检查 `/space` 显示“账号中心”，顶部账号 `admin` 链接到 `/space`，页面无旧“个人空间”和“打开公开页”文案，保存资料后显示“账号资料已保存”
- 【✔】 调整 2026-07-02 15:45 四库隔离设计；结果：后端从单库 `user_account` 改为四库配置，网站业务库 `qfc_site` 保存 `site_user`、项目和文件元数据，网站日志库 `qfc_site_log` 预留网站登录/操作日志，后台管理库 `qfc_admin` 保存 `admin_user`、角色和权限，后台日志库 `qfc_admin_log` 预留后台登录/操作/控制台日志；登录态继续返回 `SITE_USER` / `ADMIN`，后台拦截器先校验账号类型再查后台权限，避免网站用户和后台管理员 ID 重复时串权；影响范围：`server/src/main/java/com/qfc/config`、`server/src/main/java/com/qfc/auth`、`server/src/main/java/com/qfc/admin`、`server/src/main/java/com/qfc/user`、`server/src/main/resources`、`server/src/test/java/com/qfc`、`docs/`；验证：先新增多数据源、账号拆分和后台拦截器失败测试，再实现通过；`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 30, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 通过；`npm --prefix admin-web run build` 通过
- 【✔】 修正 2026-07-02 15:27 后台标准布局；结果：`admin-web/` 从居中内容页和页面内 tab 改为标准管理后台 Shell，左侧为模块路由栏，右侧为顶部操作区和路由展示区域；新增 `/site-users`、`/admin-users`、`/roles` 模块路由，`/` 重定向到 `/site-users`；无 `ROLE_MANAGE` 权限的后台账号不能进入管理员和角色模块；影响范围：`admin-web/src/App.vue`、`admin-web/src/router/index.ts`、`admin-web/src/views/AdminDashboardView.vue`、`admin-web/src/style.css`、`docs/design.md`、`docs/development.md`；验证：`npm --prefix admin-web run build` 通过；浏览器检查未登录访问 `/` 重定向到 `/login?redirect=/site-users`，登录页无控制台错误；受后端当前未登录返回 `401` 限制，登录后后台 Shell 需用后台账号继续目视验收
- 【✔】 修正 2026-07-02 15:29 文件移动和目录选择交互；结果：后端新增 `PUT /api/admin/files/{fileId}/path`，支持将文件移动到当前项目已有目录或项目根目录，移动时同步更新 `relativePath` 和 `storagePath` 并移动本地文件，目标目录有同名文件时返回 `FILE_PATH_EXISTS`；账号中心文件行新增“移动”，弹窗改为从当前项目已有目录选择目标，包含“项目根目录”；修复本地保存移动失败，根因是 `8081` 旧后端进程未加载新接口，重启并使用本机配置文件后验证成功；影响范围：`server/src/main/java/com/qfc/file`、`server/src/test/java/com/qfc/file/FileServiceTest.java`、`web/src/api/admin.ts`、`web/src/utils/fileTree.ts`、`web/tests/fileTree.test.mjs`、`web/src/components/FileTreeList.vue`、`web/src/views/SpaceView.vue`、`web/src/style.css`、`docs/`；验证：`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 27, Failures: 0, Errors: 0, Skipped: 0`；`node --experimental-strip-types tests\fileTree.test.mjs src\utils\fileTree.ts` 通过；`npm --prefix web run build` 通过；直连 `8081` 和经 `5173` 代理调用 `PUT /api/admin/files/35/path` 均返回 `SUCCESS`
- 【✔】 修复 2026-07-02 15:59 网站和后台同名 admin 账号分流；结果：主站登录 `/api/auth/login` 只查询 `qfc_site.site_user`，后台登录 `/api/auth/admin/login` 只查询 `qfc_admin.admin_user`，两张表允许同名账号；`data.sql` 和四库迁移脚本都会初始化网站用户 `admin / 123456`，并保留后台管理员 `admin / 123456`；已对本机 Docker MySQL 执行幂等 `schema.sql`、`data.sql`，确认 `qfc_site.site_user` 和 `qfc_admin.admin_user` 均存在 `admin`；影响范围：`server/src/main/java/com/qfc/auth`、`server/src/main/java/com/qfc/admin/UserManagementService.java`、`server/src/main/resources/db`、`server/src/test/java/com/qfc`、`admin-web/src/api/auth.ts`、`docs/`；验证：先写登录分流、同名账号和种子数据失败测试，再实现通过；接口实测主站 `admin / 123456` 返回 `SITE_USER`，后台 `admin / 123456` 返回 `ADMIN`，`GET /api/admin/site-users` 返回 1 个网站用户 `admin`；`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 34, Failures: 0, Errors: 0, Skipped: 0`；`npm --prefix web run build` 和 `npm --prefix admin-web run build` 通过
- 【✔】 修正 2026-07-02 16:25 后台管理 UI 组件库和用户管理列表形态；结果：`admin-web/` 接入 Element Plus 和 `@element-plus/icons-vue`，后台登录页改为 Element Plus 卡片表单，后台 Shell 改为 `el-container`、`el-menu`、`el-header`，网站用户和后台管理员模块改为列表优先的 `el-table` 页面，新增和编辑通过 `el-dialog` 弹窗进入，不再在用户管理页常驻新增表单；角色权限模块改为 Element Plus 表格、卡片、表单和复选框组；影响范围：`admin-web/package.json`、`admin-web/package-lock.json`、`admin-web/src/main.ts`、`admin-web/src/App.vue`、`admin-web/src/views/AdminLoginView.vue`、`admin-web/src/views/AdminDashboardView.vue`、`admin-web/src/style.css`、`docs/`；验证：`npm --prefix admin-web run build` 通过；浏览器检查 `http://127.0.0.1:5174/site-users` 默认显示用户列表表格和“新增用户”按钮，不显示常驻“初始密码”表单，点击“新增用户”后才出现“新增网站用户”弹窗

## 需求：修复主站和后台账号权限边界混用 <2026-07-02 17-41-59>：

- 【✔】 阶段 1：同步问题和目标边界；结果：明确主站账号中心项目管理属于网站用户功能，后台 `PROJECT_MANAGE` 只属于后台管理系统；已更新 `docs/requirements.md`、`docs/design.md`、`docs/development.md` 记录站点边界、独立 session、项目 owner 和 `/api/space/**` 接口；影响范围：`docs/`；验证：重新扫描当前文档，无“项目管理权限”“同时作为第一版项目管理账号”等旧边界文案残留
- 【❓】 问题记录：用户反馈“用户网站的账号中心没有个人项目管理模块”，并指出前两轮把用户网站的 `admin` 和后台网站的 `admin` 当成同一个角色；当前处理：按主站网站用户和后台管理员两套角色拆分修复
- 【✔】 阶段 2：补充失败回归测试；结果：新增 `AuthControllerSessionTest`、`SpaceProjectControllerTest`、项目 owner 相关服务测试、主站/后台前端互跳入口文本回归测试和主站 `space` API 测试；影响范围：`server/src/test/java`、`web/tests`、`admin-web/tests`；验证：首次运行按预期失败，暴露新 session key、`SpaceProjectController`、owner 字段和前端入口/API 尚未实现
- 【✔】 阶段 3：实现后端独立 session 与 `/api/space/**` 主站项目接口；结果：新增 `QFC_SITE_LOGIN_USER` 和 `QFC_ADMIN_LOGIN_USER`，主站 auth 只读写网站 session，后台 auth 只读写后台 session；项目新增 `owner_user_id`，主站项目接口只操作当前网站用户自己的项目，后台 `/api/admin/**` 保留为后台管理接口；影响范围：`server/src/main/java/com/qfc/auth`、`server/src/main/java/com/qfc/config`、`server/src/main/java/com/qfc/project`、`server/src/main/java/com/qfc/file`、`server/src/main/resources/db`；验证：目标后端测试通过，`Tests run: 27, Failures: 0, Errors: 0, Skipped: 0`
- 【✔】 阶段 4：移除主站和后台互跳入口，调整主站账号中心恢复项目管理显示并改用 `/api/space/**`；结果：主站删除“返回后台”和登录页后台入口，后台删除“打开主站”，后台前端改用 `/auth/admin/me` 与 `/auth/admin/logout`；账号中心项目管理登录后显示，项目 API 改为 `web/src/api/space.ts`，删除主站旧 `web/src/api/admin.ts`；影响范围：`web/src`、`admin-web/src`；验证：主站和后台前端回归测试通过
- 【✔】 阶段 5：运行后端测试、前端回归测试和两个前端构建；结果：后端全量测试、主站 Node 回归测试、后台 Node 回归测试、主站构建、后台构建均通过；影响范围：整体回归；验证：`mvn -f server/pom.xml test -B -ntp` 通过，`Tests run: 46, Failures: 0, Errors: 0, Skipped: 0`；`npm run build` 在 `web/` 和 `admin-web/` 均通过，后台构建仅有依赖包 pure annotation 和大 chunk 体积警告
- 【✔】 运行修复 2026-07-02 18:42：处理用户反馈“admin 显示 0 个项目，实际应有小画文档”；结果：确认 8081 后端仍是 15:58 启动的旧进程，`GET /api/space/projects` 返回 404；执行 `migration-2026-07-02-space-project-owner.sql` 为旧项目补 `owner_user_id`，`小画文档` 归属到网站用户 `admin`；重启后端后 `/api/space/projects` 不再 404；影响范围：本机 `qfc_site.project` 数据、8081 后端运行进程；验证：直连 `8081` 和经 `5173` 代理登录 `admin / 123456` 后均返回 `projectCount=1`，项目为 `小画文档 / xiaohua`

## 需求：明确 Word 文档预览边界 <2026-07-03 16-05-27>：

- 【✔】 阶段 1：处理 DOCX 图片预览体验；结果：Word 预览保留 POI 文本预览，页面新增“仅展示文本预览”提示，说明图片和复杂版式需下载原文件查看；影响范围：`web/src/views/FilePreviewView.vue`、`web/src/style.css`、`web/tests/filePreviewTemplate.test.mjs`；验证：`node web\tests\filePreviewTemplate.test.mjs` 通过，`npm --prefix web run build` 通过
- 【✔】 阶段 2：同步项目文档；结果：更新 Word 预览范围、设计策略和开发说明，保留后续接入高保真 DOCX 渲染方案的空间；影响范围：`docs/requirements.md`、`docs/design.md`、`docs/development.md`、`docs/progress.md`、`docs/superpowers/specs/2026-07-01-quiet-fleet-collection-design.md`；验证：重新扫描当前文档中的 Word/DOCX 预览描述

## 需求：修复安全与健壮性问题并优化本地启动脚本 <2026-07-03 20-34-21>：

- 【✔】 阶段 1：核查外部 bug 清单；结果：确认 CSRF 缺失、登录未更换 Session ID、项目 slug 重复返回 500、运行时异常兜底不足、后台管理员角色编辑只保留首个角色、后台账号误入主站空间、上传 MIME 可信度不足等问题成立；同时确认 Vue 文本插值默认会转义，Issue/反馈列表不是 `v-html` 存储型 XSS 触发点，角色新增接口禁用和反馈状态接口缺失属于业务范围；影响范围：后端 Controller/Service/Interceptor、主站和后台 axios、后台管理员表单、启动脚本；验证：逐项读取相关源码并按实际渲染方式、接口边界和数据库约束复核
- 【✔】 阶段 2：实现安全与健壮性修复；结果：新增 `/api/auth/csrf` 和 `X-CSRF-Token` 校验，前端 unsafe method 自动取 token 并重试一次；登录成功后调用 `changeSessionId()`；项目创建/编辑校验 slug 唯一和 visibility；全局异常兜底返回 `INTERNAL_ERROR`；头像按文件魔数识别，文件 inline 响应添加 `X-Content-Type-Options: nosniff`；后台管理员角色表单改为完整 `roleCodes` 多选；主站空间仅允许 `SITE_USER`；影响范围：`server/src/main/java/com/qfc`、`web/src/api/http.ts`、`admin-web/src/api/http.ts`、`admin-web/src/views/AdminDashboardView.vue`、`web/src/router/spaceAccess.ts`；验证：`mvn -f server\pom.xml test -B -ntp` 通过，`npm --prefix web run build` 通过，`npm --prefix admin-web run build` 通过
- 【✔】 阶段 3：优化本地脚本并重启后端；结果：新增可双击执行的 `scripts/start-dev.bat`、`scripts/restart-backend.bat`、`scripts/organize-logs.bat` 和后端专用 `scripts/restart-backend.ps1`，`start-dev.ps1` 启动前会清理本项目占用 `8081/5173/5174` 的旧进程；已重启后端并确认后台登录可用；影响范围：`scripts/`、本机 8081 后端进程、`README.md`；验证：`GET http://127.0.0.1:8081/api/auth/csrf` 返回 `SUCCESS` 且 token 长度 36，携带 token 调用后台登录 `admin / 123456` 返回 `SUCCESS / ADMIN / admin`，经 `5174` 代理访问 `/api/auth/csrf` 返回 `SUCCESS`
- 【✔】 阶段 4：同步项目文档；结果：补充安全要求、CSRF/session 行为、文件 MIME 和 `nosniff` 策略、本地 bat 脚本使用说明以及本轮验证记录；影响范围：`docs/progress.md`、`docs/development.md`、`docs/design.md`、`docs/requirements.md`；验证：重新扫描文档中的 `CSRF`、`restart-backend`、`start-dev.bat`、`nosniff`、`visibility` 等关键词
